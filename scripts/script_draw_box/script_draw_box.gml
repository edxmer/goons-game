


/**
 * Draw a 
 * @param {real} _x The x (game) position for the top left corner
 * @param {real} _y The y (game) position for the top left corner
 * @param {real} horizontal The horizontal size (px)
 * @param {real} vertical The vertical size (px)
 */
function draw_box(_x, _y, horizontal, vertical, size, sprite) {
	
}